export const metadata = {
  title: '赛事日历 - MTG Modern Stats',
  description: '摩登赛制 upcoming tournaments and events',
};

export default function CalendarLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
